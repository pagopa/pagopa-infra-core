pr:
  autoCancel: true
  branches:
    include:
      - main
  paths:
    include:
      - 'src/tf-audit'
    exclude:
      - '**/*.lock.hcl'
      - '**/*.md'

parameters:
  - name: 'PROD'
    displayName: 'Run on PROD environment'
    type: boolean
    default: True
    values:
      - False
      - True



pool:
  vmImage: 'ubuntu-latest'

resources:
  repositories:
    - repository: terraform
      type: github
      name: pagopa/azure-pipeline-templates
      ref: refs/heads/feat-tf-debug
      endpoint: "io-azure-devops-github-ro"

variables:
  TIME_OUT: 15
  # working dir
  WORKING_DIR: 'src/tf-audit'
  REGION_SHORT: "weu"

stages:
  #PROD
  - ${{ if eq(parameters['PROD'], true) }}:
      - template: './base-code-review-pipelines.yml'
        parameters:
          ENV: "prod"
          ENV_FOLDER: "weu-prod"
          WORKING_DIR_MODULE: ${{ variables.WORKING_DIR }}
          SC_PLAN_NAME: '$(TF_AZURE_SERVICE_CONNECTION_PLAN_NAME_PROD)'
          POOL_NAME: '$(TF_POOL_NAME_PROD)'
          TIME_OUT: ${{ variables.TIME_OUT }}
          TF_SUMMARIZE: true
          SECRET_USE_AKS: false




